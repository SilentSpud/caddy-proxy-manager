# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies. Unanchored: the isolated linker gives every workspace package its
# own node_modules of symlinks, not just the repo root.
node_modules/

# testing. Unanchored: build and report output now lands inside each workspace
# package, not only at the repo root.
coverage/

# next.js
.next/
.vinext/
dist/
out/
.vite/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# ai stuff
/.claude/

# configs for running instances
/caddy-data
/caddy-config
/data
/apps/controller/data

# Playwright auth state
apps/controller/tests/.auth/
test-results/
playwright-report/

/geoip-data

# Temp SQLite databases left behind by tests/integration/db-compat-accounts.test.ts on a
# failure, before its cleanup runs.
tmp-db-*/

# The 3.0 SQLite database the migration e2e spec builds; the directory is tracked, not its
# contents.
apps/controller/tests/.legacy/*
!apps/controller/tests/.legacy/.gitkeep
