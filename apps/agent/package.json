{
  "name": "@cpm/agent",
  "version": "3.0.0-beta.1",
  "description": "Caddy Proxy Manager agent — manages the Caddy container on its host",
  "type": "module",
  "private": true,
  "dependencies": {
    "@cpm/shared": "workspace:*"
  }
}
