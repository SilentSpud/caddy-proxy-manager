/** Its own leaf so the dashboard's client component can write the cookie without the schema. */
export const SQLITE_NOTICE_COOKIE = "cpm-sqlite-notice-dismissed";
export const SQLITE_NOTICE_DISMISS_SECONDS = 7 * 24 * 60 * 60;
