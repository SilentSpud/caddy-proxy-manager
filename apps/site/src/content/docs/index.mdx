---
title: Caddy Proxy Manager
description: A modern web interface for Caddy Server — reverse proxy, WAF, automatic HTTPS, mTLS, forward auth, geo blocking, L4 TCP/UDP proxying, traffic analytics and a GraphQL API.
template: splash
# Starlight appends the site title to every page title, which on this page is the same
# string twice. Overriding the tag outright is the only way to say it once.
head:
  - tag: title
    content: Caddy Proxy Manager — control every edge
hero:
  tagline: Control every edge. The modern web interface for Caddy Server.
  # No hero image: the dashboard below is the real one, running, which is a better first
  # impression than a picture of it and cannot go stale.
  actions:
    - text: Install it
      link: start/install/
      icon: right-arrow
    - text: View source
      link: https://github.com/SilentSpud/caddy-proxy-manager
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';
import Demo from '../../components/Demo.astro';
import OverviewDemo from '../../demos/samples/OverviewDemo';

<Demo title="The dashboard" note="Every page on this site works like this one.">
  <OverviewDemo client:visible />
</Demo>

Caddy is a web server that gets TLS right by default. Caddy Proxy Manager is a UI in front of it, so
the things Caddy can do — reverse proxying, certificates, a WAF, layer‑4 streams — become things you
configure and watch rather than things you write into a config file and hope about.

## What it does

<CardGrid>
  <Card title="Reverse proxy" icon="random">
    Multiple upstreams, twelve load-balancing policies, health checks, custom headers, location
    rules, redirects, rewrites and upstream DNS pinning.
  </Card>
  <Card title="L4 TCP/UDP proxy" icon="puzzle">
    Layer 4 stream proxying. TLS SNI matching, PROXY protocol, health checks and geo blocking below
    HTTP.
  </Card>
  <Card title="Automatic HTTPS" icon="approve-check">
    ACME through Let's Encrypt or any directory you point it at, DNS‑01 across twenty providers,
    plus a built-in CA for mutual TLS.
  </Card>
  <Card title="WAF" icon="warning">
    Coraza with the OWASP Core Rule Set. Per-host control, rule suppression, and a searchable event
    log that links straight to the rule that fired.
  </Card>
  <Card title="Traffic analytics" icon="bars">
    Live request charts, protocol breakdown, a country map, top user agents and blocked requests,
    backed by ClickHouse.
  </Card>
  <Card title="Everything audited" icon="list-format">
    Every configuration change is recorded with the account that made it, and searchable.
  </Card>
</CardGrid>

## Start here

<CardGrid>
  <LinkCard title="What it is" href="start/what-it-is/" description="How the pieces fit together, and what it deliberately is not." />
  <LinkCard title="Install" href="start/install/" description="One compose file, two values to fill in." />
  <LinkCard title="First run" href="start/first-run/" description="The setup flow, and what it decides for you." />
</CardGrid>

## Every feature

<CardGrid>
  <LinkCard title="Reverse proxy" href="features/reverse-proxy/" />
  <LinkCard title="L4 TCP/UDP proxy" href="features/l4-proxy/" />
  <LinkCard title="Certificates & HTTPS" href="features/certificates/" />
  <LinkCard title="Dashboard host" href="features/dashboard-host/" />
  <LinkCard title="WAF" href="features/waf/" />
  <LinkCard title="Geo blocking" href="features/geo-blocking/" />
  <LinkCard title="Access lists & mTLS" href="features/access-control/" />
  <LinkCard title="Forward auth" href="features/forward-auth/" />
  <LinkCard title="Analytics" href="features/analytics/" />
  <LinkCard title="Users, roles & groups" href="features/users-and-groups/" />
  <LinkCard title="The agent" href="features/agent/" />
  <LinkCard title="Caddy Build" href="features/caddy-build/" />
  <LinkCard title="Tailscale" href="features/tailscale/" />
  <LinkCard title="API" href="features/api/" />
  <LinkCard title="Audit log" href="features/audit-log/" />
</CardGrid>
