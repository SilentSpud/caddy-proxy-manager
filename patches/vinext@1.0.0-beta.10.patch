diff --git a/dist/server/request-pipeline.js b/dist/server/request-pipeline.js
index 5a3cade..a5cf5b1 100644
--- a/dist/server/request-pipeline.js
+++ b/dist/server/request-pipeline.js
@@ -361,6 +361,8 @@ function cloneRequestWithHeaders(request, headers) {
 	let cloned;
 	try {
 		cloned = new Request(request, { headers });
+		// Bun copies a non-native Request (srvx wraps Node requests in dev) as GET when given an init.
+		if (cloned.method !== request.method) throw new TypeError("request method lost in clone");
 	} catch {
 		const init = {
 			method: request.method,
@@ -397,6 +399,7 @@ function cloneRequestWithUrl(request, url) {
 	let cloned;
 	try {
 		cloned = new Request(url, request);
+		if (cloned.method !== request.method) throw new TypeError("request method lost in clone");
 	} catch {
 		const init = {
 			method: request.method,
