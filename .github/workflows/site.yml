name: Site

on:
  # Only when the site or the code it embeds changes. The rest of the repository changes far more
  # often, and rebuilding a static site for a change to the agent would spend a runner to publish
  # the same bytes. `workflow_dispatch` covers the case where you want it published anyway.
  #
  # The controller is here because the docs render its components live, so a change to one of them
  # changes the published site. The whole of `src/` rather than the handful of directories a demo
  # reaches today: that list would go stale the first time a demo imports something new, and it
  # would go stale silently, leaving the docs showing a component the product no longer has.
  push:
    branches:
      - main
    paths:
      - "apps/site/**"
      - "apps/controller/src/**"
      - "apps/controller/messages/**"
      - ".github/workflows/site.yml"
  # The same paths, built and tested but not published, so a broken demo fails its pull request
  # rather than the deploy after it merged.
  pull_request:
    paths:
      - "apps/site/**"
      - "apps/controller/src/**"
      - "apps/controller/messages/**"
      - ".github/workflows/site.yml"
  workflow_dispatch:

# Least privilege: `contents: read` is all the build requires. The deploy job asks for the two
# Pages needs itself, so a pull request's run never holds them.
permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          # The oven/bun tag in docker/web and docker/agent, so CI tests what the images build with.
          bun-version: 1.4.2

      # The whole workspace: apps/site is a workspace member, so its dependencies come from the
      # root lockfile rather than one of its own.
      - name: Install dependencies
        run: bun install --frozen-lockfile

      # Before the build: it is seconds, and a demo copying stale controller data fails here with
      # the field named rather than as a wrong-looking form later.
      - name: Unit tests
        run: bun run --filter @cpm/site test

      - name: Build the site
        run: bun run --filter @cpm/site build

      # From apps/site, the workspace that declares @playwright/test, so bunx resolves the pinned
      # CLI rather than downloading the latest - see the same note in e2e.yml.
      - name: Resolve the Playwright version
        id: playwright
        working-directory: apps/site
        run: echo "version=$(bunx playwright --version | awk '{ print $2 }')" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.playwright.outputs.version }}-chromium

      - name: Install Chromium
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        working-directory: apps/site
        run: bunx playwright install chromium

      # Apt packages outside the cache, so this runs on a hit too; a no-op once present.
      - name: Install Chromium system dependencies
        working-directory: apps/site
        run: bunx playwright install-deps chromium

      # Every demo on every built page hydrates without an error.
      - name: Demo smoke test
        working-directory: apps/site
        run: bunx playwright test

      - name: Upload the artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: apps/site/dist

  deploy:
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    # `pages` to publish and `id-token` for the deployment's OIDC exchange.
    permissions:
      pages: write
      id-token: write
    # One deployment at a time, and never cancel one in flight: a half-finished deploy would leave
    # the published site inconsistent, and the next push will supersede it anyway.
    concurrency:
      group: pages
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5.0.1
